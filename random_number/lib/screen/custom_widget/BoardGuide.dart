import 'package:flutter/material.dart';
class BoardGuide extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Material(child: Container(child: Center(child: Text('Guide How To Play'),),),);
  }
}
